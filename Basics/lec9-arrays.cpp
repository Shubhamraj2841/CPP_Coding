#include <iostream>
using namespace std;

int main(){

    // initialisation of an array
    // type name_of_array [size_of_array]
    int num_arr[10] = {1, 2, 3};

    // indexing starts with 0
    // accessing values of array
    cout << num_arr[6];

    return 0;
}
